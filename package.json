{
  "name": "exam-monitoring-system",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "postinstall": "cd frontend && npm install",
    "build": "cd frontend && npm run build",
    "start": "cd frontend && npm run build && npm run preview"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  }
}
